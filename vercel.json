
{
    "version": 2,
    "builds": [
      {
        "src": "./dist/src/server.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "./dist/src/(.*)",
        "dest": "./dist/"
      }
    ]
}